# 3.1 운영체제와 컴퓨터

### 운영체제의 목적

- 사용자가 컴퓨터를 편리하고 효율적으로 다룰 수 있도록, 시스템 자원을 관리하는 소프트웨어

```
💡 펌웨어란? 운영체제와 유사하나, 하드웨어 내부에 포함되어 소프트웨어를 설치할 수 없는 소프트웨어
```

### 운영체제의 역할

- **CPU 스케쥴링**: CPU 소유권을 어떤 프로세스에 할당할지 결정
- **프로세스 관리**: 자원을 어떤 프로세스에 할당할지 결정 & 프로세스 생성/삭제/중단/재개
- **메모리 관리**: 메모리를 어떤 프로세스에 얼만큼 할당할지 결정
- **디스크 파일 관리**
- **I/O 디바이스 관리**

### 운영체제의 구성

```
💡 사용자 ↔ 응용 프로그램 ↔ **인터페이스 ↔ 시스템콜 ↔ 커널** ↔ 하드웨어
```

- **인터페이스**
    - 사용자가 프로그램을 직접 제어하고 사용할 수 있게 하는 인터페이스
    - ex. GUI(ex. 바탕화면 그래픽), CUI(ex. 명령 프롬프트)
- **시스템콜(syscall)**
    
    ```
    💡 운영체제는 시스템 보안을 위해 `사용자 모드`와 `커널 모드`로 나뉘어 동작한다.
    `사용자 모드`는 컴퓨터 자원에 함부로 접근하지 못하도록 접근 가능한 영역을 제한한 모드이고,
    `커널 모드`는 모든 컴퓨터 자원에 접근 가능한 모드이다.
    ```
    
    ```
    💡 `modebit`가 0이면 커널 모드, 1이면 사용자 모드
    ```
    
    - 사용자 모드에서 컴퓨터 자원에 접근하려고 할 때 사용하는 인터페이스
    - 시스템콜을 통해 사용자 모드에서 커널 모드로 전환이 이루어지며, 명령을 수행하면 다시 사용자 모드로 전환됨
    - ex. `fork()`, `exit()`, `read()`, `write()`, `pipe()`
- **커널**
    - 운영체제의 핵심으로 컴퓨터 하드웨어에 대한 가장 기초 수준의 제어권을 가짐
    - 보안, 자원 관리 등 수행
    - ex. 메모리를 얼마나 쓸지, 어떤 프로세스를 실행할지, I/O 디바이스 제어 등

### 컴퓨터 구조

컴퓨터는 CPU, 기억장치, I/O 디바이스로 구성되어 있다. 각 요소는 common 버스로 연결되어 있다. 각 요소들은 각자 장치를 관리하기 위한 디바이스 컨트롤러를 가지며, 로컬 버퍼를 통해 정보를 임시적으로 저장할 수 있다.

- CPU: 메모리에 적재된 프로세스의 명령어를 실행하는 중앙 처리 장치
    - `ALU(산술논리연산장치)`: 산술 연산(덧셈, 뺄셈)과 논리 연산(논리합, 논리곱)을 계산하는 장치
    - `레지스터`: CPU 내 임시 기억장치. CPU와 붙어 있어 연산 속도가 빠르고 용량은 적음.
    - `CU(제어장치)`: 레지스터에 데이터를 로드하고 ALU에 계산을 명령하는 등 프로세스 조작을 지시하는 장치
- 메모리 (3.2 참고)
- I/O 디바이스

### 인터럽트

- 예정되지 않은 어떤 요청이 들어왔을 때, 현재 파이프라인을 멈추고 해당 요청을 먼저 해결하는 방식
- 인터럽트 발생 시 인터럽트 핸들러 함수를 실행하여 처리함
    - 프로세서가 인터럽트를 감지하면 현재 상태를 저장해두고 인터럽트 스레드로 넘어가서 처리
    - 인터럽트 처리 후 기존 명령어를 수행하기 위해 다시 돌아옴
    - 만약 계속 수행할 수 있으면 수행하고, 불가능하면(ex. 세그먼트 폴트) 오류 보고나 프로그램 종료 등 수행
- 여러 인터럽트 간 우선순위 존재
- 인터럽트 유형
    - Exception: 특정 오류에 대한(sync) 인터럽트
    - Interrupt: 언제 일어날지 모르는(async) 인터럽트
    - 시스템 콜/트랩 명령어
- 인터럽트 종류
    - 하드웨어 인터럽트: I/O 디바이스 연결 등으로 OS에 시스템콜을 요청할 때
    - 소프트웨어 인터럽트: 프로세스 오류 등 프로세스가 시스템콜을 호출할 때

# 3.2 메모리

### 메모리 계층

- 비교표
    
    |  | 개념 | 휘발성 | 속도 | 용량 | 예시 |
    | --- | --- | --- | --- | --- | --- |
    | 레지스터 | CPU 내 임시기억장치 | O | 매우 빠름 | 매우 작음 |  |
    | 캐시 | 빠른 장치와 느린 장치 간 병목 현상을 줄이기 위한 메모리 | O | 빠름 | 작음 | L1, L2 캐시 |
    | 주기억장치 | CPU에 빠르게 전달하기 위해 데이터를 임시 저장하는 메모리 | O | 보통 | 보통 | RAM, ROM |
    | 보조기억장치 | 데이터를 영구 보관하기 위한 메모리  | X | 느림 | 큼 | HDD, SSD, USB … |

### 캐시

- 데이터를 미리 복사해 놓는 메모리
- 레지스터는 메모리-CPU 간 캐시 계층, 메모리는 레지스터-보조기억장치 간 캐시 계층이라고 할 수 있음
- 자주 사용하는 데이터를 저장하여 효율을 극대화
- 캐시히트와 캐시미스
    - 캐시히트: 원하는 데이터를 캐시에서 찾는 경우
    - 캐시미스: 원하는 데이터를 캐시에서 찾지 못해 주기억장치로 가 데이터를 찾는 경우
    - 캐시매핑: 캐시가 히트되기 위해 매핑하는 방법
- 웹 브라우저의 캐시
    - 쿠키: 키-값 쌍 저장, 4KB 크기, 만료 기한 O
    - 로컬 스토리지: 키-값 쌍 저장, 5MB 크기, 영구 저장 가능
    - 세션 스토리지: 키-값 쌍 저장, 5MB 크기, 세션 닫을 시 데이터 삭제
    

### **지역성(Locality)**

자주 사용하는 데이터의 근거가 됨

- 시간 지역성(Temporal Locality): “최근” 참조한 데이터 영역에 다시 접근하는 특성 (ex. 루프)
- 공간 지역성(Spatial Locality): 참조한 데이터와 “인접한” 데이터에 접근하는 특성 (ex. 배열)

### 메모리 할당 방식

- 연속 할당: 메모리에 프로세스를 연속적으로 할당하는 것
    - **고정 분할 방식**: 메모리를 미리 나눠 놓고 프로세스를 할당 ⇒ 내부 단편화 발생
    - **가변 분할 방식**: 메모리를 프로세스 크기에 맞게 동적으로 나눔 ⇒ 외부 단편화 발생
        - 최초적합(First Fit): 위쪽이나 아래쪽부터 시작해 홀을 찾으면 바로 할당
        - 최적적합(Best Fit): 프로세스 크기 이상인 공간 중 가장 작은 홀에 할당
        - 최약적합(Worst Fit): 프로세스 크기와 가장 많이 차이나는 홀에 할당
    
    ```
    💡
    - 내부 단편화: (고정된 크기의 파티션 - 프로세스 크기) 만큼 홀이 생겨, 빈 공간이 있지만 쓰지 못하는 문제 발생
    - 외부 단편화: (이전에 할당된 프로세스 크기)만큼 홀이 생김 ⇒ 각 홀의 크기의 합이 적재하려는 프로세스 크기보다 커도, 연속적으로 할당할 수 없어 실제로는 쓰지 못하는 문제 발생
    ```
    
- 불연속 할당: 메모리에 프로세스를 연속적으로 할당하지 않는 것
    - **페이징**: 실제 및 가상 메모리를 동일한 크기의 블록으로 나누어(각각 프레임/페이지라 명명, 보통 4KB), 빈 프레임들에 프로세스를 나누어 할당(like 고정 분할)
    - **세그멘테이션**: 프로세스를 여러 세그먼트로 나누고 이를 메모리에 동적으로 할당(like 가변 분할)
        
        ```
        💡
        세그먼트는 함수 하나가 될 수도 있고, 프로세스를 이루는 코드 영역이나 데이터 영역이 될 수도 있음
        ```
        
    - **페이지드 세그멘테이션**: 프로세스를 세그먼트로 나누고, 각 세그먼트를 페이지 단위로 다시 나눔
        - 장점: 세그멘테이션 장점인 보호와 공유가 용이하고, 외부 단편화를 방지
        - 단점: 세그먼트 테이블 및 페이지 테이블 필요 ⇒ 주소 변환 복잡, 소요 시간 ↑

### 가상 메모리와 페이징

```
💡
**배경 설명**
- 개발자가 실제 메모리 주소(physical address)에 직접 접근하는 방식은 불편하고 어렵다.
- 멀티태스킹을 지원하기 위해서는 기존의 메모리 관리 방식으로 한계가 있었다.
- 사용자가 오고 감에 따라 메모리는 파편화되기 마련인데, 파편의 크기가 작아서 데이터를 통째로 넣지 못하는 경우가 생길 수 있다.
```

- 개념
    - 메모리 관리 기법 중 하나
    - 컴퓨터가 실제로 이용 가능한 메모리 자원을 추상화하여 사용자에게 매우 큰 메모리로 보이게 함
    - 접근 제어를 통해 메모리를 보호함
    
    ```
    💡
    1. 프로세스마다 가상 주소(logical address) 공간을 갖는다.
    2. 프로세스마다 페이지 테이블을 갖는다. (이는 실제 메모리 공간에 저장된다.)
    3. 페이지 테이블에는 가상 주소와 실제 주소(physical address) 간 맵핑 정보가 포함된다.
    4. 프로세스가 가상 주소에 접근하면 MMU(메모리 관리 장치)가 이를 실제 주소로 변환한다.
    ```
    
- 페이지 교체(Swapping)
    - 더이상 남은 메모리가 없을 때, 메모리에서 당장 사용하지 않는 영역을 디스크로 옮기고, 디스크의 일부분을 마치 메모리처럼 사용하는 방식
    - 메모리 과할당(실제 메모리 크기보다 큰 크기의 메모리를 프로세스에 할당하는 것)을 가능하게 함
- 페이지 폴트(Page Fault)
    - 페이지가 실제 메모리에 적재되어 있지 않은 상황에서 페이지에 접근하면 발생(=Page miss)
    - ⇒ 페이지 교체 & 페이지 테이블 갱신 후 명령어 다시 시작

# 3.3 프로세스와 스레드

### 프로세스, 스레드 개념

- **프로세스**
    - 실행 중인 프로그램(의 인스턴스)
    - 메모리(코드/데이터/힙/스택)와 다른 컴퓨터 리소스를 사용함
    - 운영체제는 프로세스 정보가 포함된 **PCB(프로세스 제어 블록)**를 활용해 프로세스를 스케쥴링함
    (ex. 프로세스ID, 프로세스 권한, 프로그램카운터(PC), 스케줄링 상태 등)
    - 여러 메서드를 가짐
        - `fork`: 프로세스를 복제해 child 프로세스를 생성함
        - `exec`: 현재 프로세스를 대체하는 새 프로세스를 생성할 수 있음 (exec 자체는 없고, execl, execv 등 여러 함수가 있음)
        - `exit`: 프로세스를 종료하고 리턴 값을 반환함
        - `abort`, `kill`: 부모 프로세스가 자식 프로세스를 종료하기 위해 사용함
        - `wait`: 프로세스 종료 시 반환 값을 부모가 거둬가기 위해 사용함 (⇒ 자식이 죽었는데 부모가 wait()을 안해주면 좀비 프로세스가 됨)
- **스레드**
    - 프로세스 내 작업 흐름으로, 실행 가능한 가장 작은 단위임
    - 스레드 간 프로세스의 코드/데이터/힙을 공유 (❗스택은 스레드 별로 각자 생성됨)

### 프로세스의 상태

프로세스는 실행되는 동안 여러 상태를 갖는다. 커널에는 준비 큐, 대기 큐, 실행 큐 등이 있으며 이를 이용하여 커널이 프로세스의 상태를 관리한다.
- running 상태에 돌입하면 CPU를 차지하게 된다.
- waiting 상태는 block(보류)이라고 부르기도 한다.

### 프로세스의 주소 공간

각 프로세스는 메모리에서 자신만의 주소 공간을 갖는다.

- **코드(텍스트)**: 명령어를 저장함
- **데이터**: 전역 변수를 저장함
- **힙**: 동적 할당되는 변수 또는 동적으로 관리되는 자료구조를 저장함
- **스택**: 런타임 과정에서 일시적인 데이터(ex. 지역 변수, 매개변수 등)를 동적으로 저장함

```
💡
코드/데이터 영역은 정적 할당되는 영역인 반면, 힙/스택 영역은 런타임 과정에서 동적으로 할당되는 영역으로,
저장되는 데이터에 따라 영역의 크기가 줄어들거나 늘어날 수 있다.
```

### IPC(Inter-Process Communication) 모델

IPC란 여러 프로세스 간 데이터를 공유하기 위한 메커니즘을 의미한다. IPC 구현 모델들은 다음과 같다.

- **Shared Memory(공유 메모리)**
    - 동일한 메모리 블록에 공유할 데이터를 쓰고, 각 프로세스가 읽는 방식
    - 커널이 직접 제어하지 않고, 프로세스들끼리 알아서 통신함
    - 여러 프로세스가 동시에 데이터를 쓴다거나 이미 읽었는데 데이터가 업데이트되는 등, 동기화 문제가 있음
- **Message Passing**
    - 메시지 큐를 두고 커널이 프로세스 간 메시지 전달을 도맡는 방식 (Send/Receive)
    - 마이크로커널처럼 분산된 환경에서 효과적이며, 구현이 쉬움
- **Signals**
    - 프로세스에서 특정 이벤트 발생 시 시그널을 생성하고, OS가 이를 다른 프로세스에게 알리는 방식
    - 인터럽트처럼 보일 수 있음 ⇒ 모든 시그널은 시그널 핸들러를 가짐
- **Pipes**
    - 두 개의 프로세스가 도관(파이프)을 통해 통신하는 방식
    - 종류
        - **익명 파이프**: 부모-자식 프로세스 간 단방향 통신 방식 (한 쪽은 읽기만, 한 쪽은 쓰기만 가능)
            
            ```bash
            cat data | sort | uniq -c; git diff | grep -e "^+" | wc -l
            ```
            
        - **명명된 파이프**: 두 프로세스 간 양방향 통신 방식
- **Remote Procedure Calls (RPCs)**
    - 별도의 원격 제어를 위한 코딩 없이도, 다른 프로세스의 함수/프로시저를 실행할 수 있게 하는 방식
    - 구현
        - Big-endian: 메모리에 순차적으로 쓰는 방식
        - Little-endian: 메모리에 역순으로 쓰는 방식
        - XDR(External Data Representation): 어떤 데이터든 중립되게 추상화하는 방식

### 멀티 스레드

멀티 스레드를 통해 동시성과 효율성을 모두 챙길 수 있다. 각 스레드는 각자 레지스터(프로그램 카운터)와 스택을 갖는다.

- 멀티
- 여러 스레드 간 프로세스 내 자원을 공유(스택 제외)
    - 효율성 증가
    - 한 스레드가 자원을 오염시키면 다른 스레드에도 영향을 미침
- 한 스레드가 중단되어도 다른 스레드는 실행 상태일 수 있음 ⇒ 동시성
- 무거운 프로세스 대신 가벼운 스레드를 생성해 작업을 처리할 수 있음

### 동시성과 병렬성

- **동시성(Concurrency)**: 동시에 여러 작업을 수행하는 것처럼 보이는 것
    
    ⇒ 싱글 프로세서에서 프로세스 스케줄링을 통해 멀티 프로세스의 동시성을 높임 (3.4 참고)
    
    ⇒ 스레드 **동기화 메커니즘**을 통해 동시성을 높임
    
- **병렬성(Parallelism)**: 작업을 수행하는 여러 장치가 각각 작업을 수행하는 것
    
    ⇒ 멀티 프로세서에서는 멀티 프로세스 실행이 가능
    

```
💡
**동기화 메커니즘(Synchronization)**은 동일한 주소 공간을 공유하는 프로세스 간 질서있는 실행을 보장하고, 데이터의 일관성을 유지한다. ‘Lock’ 기반의 뮤텍스, ‘Signal’ 기반의 세마포어 등의 메커니즘으로 동기화를 제공할 수 있다.
```

### 경쟁 상태와 임계 영역

- 개념
    - **경쟁 상태(Race Condition)**: 여러 프로세스가 동기화 메커니즘 없이 공유 자원에 동시에 접근하는 상황
    - **임계 영역(Critical Section)**: 여러 프로세스, 스레드가 공유 자원에 접근하는 코드 영역
- 경쟁 상태를 해결하기 위한 충족 요건 3가지
    - Mutual Exclusive: 어떤 프로세스가 임계 영역에 있으면 다른 프로세스는 진입 불가
    - Progress: 임계 영역에 아무 프로세스도 없을 때 진입하려는 프로세스가 있으면 진입하게 함
    - Bounded Waiting: 프로세스가 임계 영역에 들어가려고 요청하면 진입할 때까지 다른 프로세스의 진입은 일정 시간 제한해야 함 (⇒ 무한 양보하는 Starvation 방지)

⇒ `Lock` 메커니즘을 활용해 동기화를 제공 가능

### 뮤텍스(Mutex, Mutual Exclusive Lock)

- 프로세스나 스레드가 공유 자원을 사용할 때 lock하고, 사용한 후에는 unlock하는 방식
- **Spinlock**: 계속 문고리를 돌려보는 방식(busy-wait) ⇒ 컨텍스트 스위칭을 안하지만 CPU 소모가 심함
- **Block**: 누가 lock 하고 있으면 기다리는 방식

```
💡
뮤텍스가 “화장실이 하나뿐인 식당”이면, 세마포어는 “화장실이 여러 개인 식당”임
```

### 세마포어(Semaphore)

- 개념
    - `S`개의 공유 자원이 있고, 현재 사용중이지 않은 자원의 수를 프로세스/스레드가 볼 수 있음
    - 공유 자원에 접근하면서 S-1, 나오면서 S+1함
    - 공유 자원을 누가 쓰고 있는데 접근하면, 세마포어가 `wait()` 함수로 기다리게 함
    - 공유 자원을 다 썼고 누가 기다리고 있으면, 세마포어가 `signal()` 함수로 순서를 넘김
- 종류
    - 바이너리 세마포어: S값이 1인 경우 (뮤텍스와 비슷하지만, ‘잠금’ 기반이 아닌 ‘신호’ 기반임)
    - 카운팅 세마포어: S값이 N인 경우

### 교착 상태(Deadlock)

- 개념: 여러 프로세스가 원하는 자원이 상대방에 할당되어 있어 무한히 대기하는 상태
- 발생 조건 4가지
    - **상호배제(Mutual exclusive)**: 한 번에 한 프로세스만 자원을 사용해야 함
    - **점유대기(Hold and wait)**: 프로세스가 어떤 자원을 점유하면서, 추가 자원을 요구해야 함
    - **비선점(No preemption)**: 다른 프로세스에 할당된 자원은 뺏을 수 없음 (기다려야 함)
    - **순환 대기(Circuit wait)**: 각 프로세스는 다음 프로세스가 요구하는 자원을 가져야 함
- 해결 방법
    - 예방: 데드락 발생 조건 중 하나라도 발생되지 않게 하는 방식 ⇒ 효율 ↓
        - 상호배제 부정: 여러 프로세스가 공유 자원을 사용하게 함
        - 점유대기 부정: 프로세스 실행 전 모든 자원을 미리 할당
        - 비선점 부정: 다른 프로세스에 할당된 자원을 뺏을 수 있게 함
        - 순환 대기 부정: 자원에 고유번호 할당 후 순서대로 자원을 요구하게 함
    - 회피: 데드락 발생 시 피해나가는 방식
        - Banker’s Algorithm: 시스템이 프로세스 별 자원 요구 정보를 미리 알아두고, 자원을 할당해도 Safe 상태라면 할당
        - 만약 자원을 할당했는데 끝낼 수 있는 프로세스가 없다면 Unsafe 상태가 됨          
    - 회복: 데드락 발생 상황을 감지하고 해결하는 방식 (ex. 프로세스 종료, 자원 선점 등)
    - 무시: 데드락은 잘 발생하지 않고, 다른 해결 방법이 성능에 큰 영향을 미치므로 그냥 무시하는 방식

# 3.4 CPU 스케줄링 알고리즘

- CPU 스케줄링: CPU는 프로세스에서 해야 하는 일을 스레드 단위로 CPU에 할당함
- CPU 스케줄링 알고리즘의 목표
    - 높은 CPU 이용률 (=CPU의 유휴 시간을 최소화)
    - 단위 시간당 많은 처리량
    - 적은 대기 시간 (=프로세스가 준비 큐에서 대기하는 시간 최소화)
    - 짧은 응답 시간 (=프로세스가 요청한 후 CPU에 할당받기까지의 시간 최소화)
    - 공정성 (=특정 프로세스가 무한정 대기하지 않고 모든 프로세스가 CPU를 공평하게 씀)

### 비선점형 방식

강제로 프로세스를 중지하지 않고, 프로세스가 스스로 CPU 소유권을 포기하는 방식

- **FCFS(First Come, First Served)**
    - 가장 먼저 온 프로세스부터 처리
    - 프로세스가 처리되는 동안 기다려야 해서 대기 시간이 길어지는 문제
- **SJF(Shortest Job First)**
    - 처리 시간이 짧은 프로세스부터 처리 (과거 실행했던 시간을 토대로 처리 시간을 추측)
    - 처리 시간이 긴 프로세스가 무한정 대기하는 문제(starvation)
- **우선순위(Priority)**
    - 우선순위가 높은 프로세스부터 처리 (우선순위는 시스템 또는 사용자가 지정)
    - 우선순위가 낮은 프로세스가 무한정 대기하는 문제(starvation)
        - ⇒ 에이징(Aging): 오래 기다린 작업일수록 우선순위를 높여주는 방식
        - 책에서는 에이징을 `align`이라고 썼는데(p180), 처음 보는 말

### 선점형 방식

강제로 프로세스를 중지하고 다른 프로세스에 CPU 소유권을 할당하는 방식

- **라운드 로빈(RR, Round Robin)**
    - *현대 컴퓨터가 쓰는 스케줄링 방식*
    - 모든 프로세스에 동일한 시간을 주고 돌아가면서 처리
    - 할당 시간이 크면 FCFS가 되고, 작으면 컨텍스트 스위칭 비용이 커짐
    - 일반적으로 전체 작업 시간이 길어지나, 평균 응답 속도가 빠름
- **SRF(Shortest Remaining Time First)**
    - *SJF의 선점형 방식*
    - 처리 시간이 짧은 프로세스부터 처리 (현재 처리하는 프로세스보다 짧은 실행 시간을 갖는 프로세스가 도착하면, 현재 프로세스를 중지하고 새 프로세스를 수행)
- **다단계 큐(Multilevel Queue)**
    - 여러 큐가 각각 다른 스케줄링 알고리즘과 우선순위를 가지고 프로세스를 처리
    - 프로세스를 분리하여 각각의 특성에 맞는 스케줄링이 가능
    - 우선순위가 낮은 큐에 배치된 프로세스가 무한정 대기하는 문제(starvation)
